<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.MemberDAO">



	<insert id="insert" parameterType="dto.Member">
		insert into scheduler(id, name, point)
		values('team2', '2조', '0')
	</insert>

	
	
	<update id="update">
		update scheduler
		set point = '2000'
		where id = #{id}
	</update> 
	
</mapper>